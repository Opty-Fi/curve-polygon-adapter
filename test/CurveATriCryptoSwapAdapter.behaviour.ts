import { PoolItem } from "./types";

export function shouldBehaveLikeCurveATriCryptoSwapAdapter(token: string, pool: PoolItem): void {
  it(`${token}, pool address : ${pool.pool}, lpToken address :${pool.lpToken}`, function () {
    console.log(`TODO`);
  });
}
